node_modules/
out/
*.vsix
.DS_Store

# docs-private is a symlink into the private notes repo (../project-notes/pegboard/docs-private).
# Plans, worklogs, designs and decisions live there; never commit it here.
# docs/ (public, committed) is separate.
docs-private
